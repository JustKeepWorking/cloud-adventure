server:
  port: 8888
spring:
  profiles:
    active: native
  security:
    user:
      name: user
      password: ConfigServerPassword
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared