server:
  port: 7000
  servlet:
    context-path: /greeting
security:
  oauth2:
    client:
      clientId: greeting-service
      clientSecret: ${GREETING_SERVICE_PASSWORD}
      accessTokenUri: http://localhost:5000/uaa/oauth/token
      grant-type: client_credentials
      scope: server
